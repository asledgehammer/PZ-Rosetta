# zombie.ui.TextDrawHorizontal
package: zombie.ui
type: enum
name: TextDrawHorizontal
extends: Enum
modifiers:
  - public
fields:
  - name: Center
    modifiers:
      - public
      - static
      - final
    return:
      type: TextDrawHorizontal
  - name: Left
    modifiers:
      - public
      - static
      - final
    return:
      type: TextDrawHorizontal
  - name: Right
    modifiers:
      - public
      - static
      - final
    return:
      type: TextDrawHorizontal
methods:
  - name: __valueOf
    modifiers:
      - public
      - static
    return:
      type: TextDrawHorizontal
      notes: the enum constant with the specified name
    parameters:
      - name: name
        type: String
        notes: the name of the enum constant to be returned.
    notes: |-
      Returns the enum constant of this class with the specified name.
      The string must match
  - name: values
    modifiers:
      - public
      - static
    return:
      type: TextDrawHorizontal
      generic: TextDrawHorizontal[]
      notes: an array containing the constants of this enum class, in the order they
        are declared
    notes: |-
      Returns an array containing the constants of this enum class, in
      the order they are declared.
