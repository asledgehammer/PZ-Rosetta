# zombie.vehicles.ClipperOffset.JoinType
package: zombie.vehicles
type: enum
name: ClipperOffset.JoinType
extends: Enum
modifiers:
  - public
  - static
fields:
  - name: jtMiter
    modifiers:
      - public
      - static
      - final
    return:
      type: ClipperOffset.JoinType
  - name: jtRound
    modifiers:
      - public
      - static
      - final
    return:
      type: ClipperOffset.JoinType
  - name: jtSquare
    modifiers:
      - public
      - static
      - final
    return:
      type: ClipperOffset.JoinType
methods:
  - name: __valueOf
    modifiers:
      - public
      - static
    return:
      type: ClipperOffset.JoinType
      notes: the enum constant with the specified name
    parameters:
      - name: name
        type: String
        notes: the name of the enum constant to be returned.
    notes: |-
      Returns the enum constant of this class with the specified name.
      The string must match
  - name: values
    modifiers:
      - public
      - static
    return:
      type: ClipperOffset.JoinType
      generic: ClipperOffset.JoinType[]
      notes: an array containing the constants of this enum class, in the order they
        are declared
    notes: |-
      Returns an array containing the constants of this enum class, in
      the order they are declared.
