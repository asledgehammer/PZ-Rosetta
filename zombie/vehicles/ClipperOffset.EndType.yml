# zombie.vehicles.ClipperOffset.EndType
package: zombie.vehicles
type: enum
name: ClipperOffset.EndType
extends: Enum
modifiers:
  - public
  - static
fields:
  - name: etClosedLine
    modifiers:
      - public
      - static
      - final
    return:
      type: ClipperOffset.EndType
  - name: etClosedPolygon
    modifiers:
      - public
      - static
      - final
    return:
      type: ClipperOffset.EndType
  - name: etOpenButt
    modifiers:
      - public
      - static
      - final
    return:
      type: ClipperOffset.EndType
  - name: etOpenRound
    modifiers:
      - public
      - static
      - final
    return:
      type: ClipperOffset.EndType
  - name: etOpenSquare
    modifiers:
      - public
      - static
      - final
    return:
      type: ClipperOffset.EndType
methods:
  - name: __valueOf
    modifiers:
      - public
      - static
    return:
      type: ClipperOffset.EndType
      notes: the enum constant with the specified name
    notes: |-
      Returns the enum constant of this class with the specified name.
      The string must match
  - name: values
    modifiers:
      - public
      - static
    return:
      type: ClipperOffset.EndType
      generic: ClipperOffset.EndType[]
      notes: an array containing the constants of this enum class, in the order they
        are declared
    notes: |-
      Returns an array containing the constants of this enum class, in
      the order they are declared.
